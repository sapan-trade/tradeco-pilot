/** Central brand constants — change the name/tagline here and it propagates. */
export const BRAND = {
  name: "Clearwise",
  tagline: "Tariff codes you can defend.",
  /** Default email sender (overridden by EMAIL_FROM env if set). */
  emailFrom: "Clearwise <alerts@clearwise.app>",
} as const;
